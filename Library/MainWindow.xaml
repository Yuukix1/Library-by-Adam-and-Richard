<Window x:Class="Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="UsernameBox" Grid.Row="0" Margin="0,10,0,5" Height="30" FontSize="14" 
                 VerticalContentAlignment="Center"
                 GotFocus="UsernameBox_GotFocus" LostFocus="UsernameBox_LostFocus"/>

        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,5,0,10" Height="30" FontSize="14" VerticalContentAlignment="Center"/>

        <Button Grid.Row="2" Content="Login" Height="35" Margin="0,5" Click="Login_Click" FontWeight="Bold"/>
        <Button Grid.Row="3" Content="Register" Height="35" Margin="0,5" Click="Register_Click" FontWeight="Bold"/>
    </Grid>
</Window>
